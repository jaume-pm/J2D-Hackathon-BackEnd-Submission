version: '3.1'

services:

  # flask_app:
  #   build: # Docker file in API
  #   ports:
  #     - 17011:17011

  mongo:
    image: mongo
    restart: always
    ports:
    - 27017:27017
    

  mongo-express:
    image: mongo-express
    restart: always
    ports:
      - 8081:8081